keycloak {
  default {
    url = "http://keycloak:8080"
    tenant = "root"
    moduleClientId = "m2m-client"
    moduleClientSecret = "${your-m2m-client-secret}"
    loginClientId = "root-login-app"
    loginClientSecret = "${your-root-login-app}"
  }
}
